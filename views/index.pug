<!DOCTYPE html>
html
  head
    meta(charset="UTF-8")
    title GIF 채팅방
  body
    div F12를 눌러 console 탭과 network탭을 확인하세요
    script(src='/socket.io/socket.io.js')
    script.
      var socket=io.connect('http://localhost:8052', {
        path:'/socket.io', //서버측과 일치해야만함
        transport:['websocket']
      });
      socket.on('news', function(data){
        console.log(data);
        socket.emit('reply', 'Hello Node.js');
      });